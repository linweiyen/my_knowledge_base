# 個人研究知識庫規劃

核定日期：2026-09-10

## 目的與範圍

此工作區用於長期累積研究，讓未來能方便瀏覽、查詢、交叉引用與追溯結論。
內容包括 AIDMS 產品研究（競品、規格、技術比較與架構選型，不含產品程式碼）、
AI 研究資訊、AI coding，以及日後新增的持續性研究方向。
使用 Git/GitHub 保存版本。知識庫網站所需的輔助工具不等同 AIDMS 產品程式碼。

## 核定的目錄結構

採用「主題分類＋主題索引＋研究日期」。以下為目標結構，並非要求一次建立所有空目錄。

```text
MyKnowledgeBase/
├── AGENTS.md                       # 代理執行前必要閱讀指示
├── KNOWLEDGE_BASE_PLAN.md           # 本規劃與長期約定
├── README.md                       # GitHub 首頁說明與閱讀入口
├── index.html                      # 知識庫網站首頁
├── inbox/                          # 尚未整理的新資料
├── products/
│   ├── index.html
│   ├── gpu/                       # GPU 產品研究、規格與比較
│   │   └── index.html
│   └── aidms/
│       ├── index.html              # 產品研究總覽、結論與待研究問題
│       ├── competitors/            # 個別競品研究及橫向比較
│       ├── requirements/           # 使用情境、需求與規格研究
│       ├── architecture/           # 技術選型與架構比較
│       │   └── gpu-sharing/
│       └── decisions/              # 實際採用的決策、理由與適用條件
├── topics/
│   ├── index.html
│   ├── ai-research/
│   │   ├── index.html
│   │   ├── models/
│   │   ├── agents/
│   │   ├── evaluation/
│   │   └── inference-and-training/
│   └── ai-coding/
│       ├── index.html
│       ├── tools/
│       ├── harness-engineering/
│       └── workflows/
├── radar/                          # 有時效性的動態與追蹤線索
│   └── 2026/
│       └── 2026-09.md
├── _templates/                     # 空白寫作範本，非研究成果
└── archive/                        # 已停止維護或被取代的內容
```

只有供閱讀的內容目錄需要 `index.html`。`assets/`、`_templates/` 等輔助目錄不需要入口。
新增長期領域用 `topics/<主題>/`，新產品用 `products/<產品>/`，只在有內容時擴充分類。
分類依研究內容決定，不受匯入時的原始目錄限制。

## HTML 入口與 Markdown 正文

- `index.html`：每個內容目錄的閱讀入口，提供摘要、研究清單、核心結論、待解問題與相關主題連結。
- 不另行維護功能重複的 `index.md`。
- 根目錄保留 `README.md`，供 GitHub 儲存庫頁面顯示說明與閱讀入口。
- 新研究以 Markdown 作為主要可維護來源；研究題目資料夾內預設使用 `research.md`。
- 之後由固定流程產生 HTML 正文及目錄入口，避免 Markdown 和 HTML 各自手動維護而失去一致性。
- HTML 生成工具、版型與部署方式尚未選定，實作時再決定。
- 只有 commit HTML 到 GitHub 不會自動建立網站；如需線上瀏覽，須另外設定 GitHub Pages 或其他靜態網站服務。
- 原有 HTML 可以保留為現存原始報告並加入導覽，不要求立刻全部轉成 Markdown。
- 需要獨立完整排版閱讀版時可用 `report.html`；不要無必要地產生重複版本。

### 閱讀 HTML 時發現錯誤的修改流程

- 對於同時有 `research.md` 與生成 HTML 的研究，**內容以 Markdown 為單一來源**。事實、文字、表格內容、來源或結論有誤時，先修改 `research.md`，按實際情況更新 `updated`；只有重新核對來源後才能更新 `verified`。
- 使用者只需指出 HTML 中哪裡有錯及期望修正的內容，不必自行查找或修改 Markdown。代理應判斷錯誤性質並完成對應檔案的修改。
- **內容錯誤：**代理先修正對應的 `research.md`，隨即在同一次任務中重新產生對應 HTML、檢查內容與連結；不能只修改生成的 HTML 正文，也不能要求使用者另行提出同步請求。使用者重新整理瀏覽器後即可閱讀更新內容。這裡的「自動同步」指代理完成修改後立即同步，現階段沒有檔案變更即時監看服務。
- **純 HTML 格式問題：**例如字體、版面、樣式或表格顯示，直接修正 HTML，不必修改 Markdown 正文。若該 HTML 由工具產生，也應視需要同步調整產生工具或版型，避免下次重新產生時覆蓋這次的格式修正。
- 若是原本只有 HTML、沒有對應 Markdown 來源的舊報告，直接修改該 HTML，保留既有內容與使用者修改。日後若建立 Markdown 來源，再明確界定單一來源與生成流程。

單份研究的建議結構：

```text
products/aidms/architecture/gpu-sharing/
├── index.html       # 研究入口／生成的閱讀頁面
├── research.md      # 新研究的可維護正文
├── report.html      # 可選：舊報告或獨立排版版本
└── assets/          # 圖片、附表及必要附件
```

## 研究筆記規範

每份研究建議包含：

1. 研究問題與摘要：回答什麼、目前結論。
2. 依據與限制：來源連結、適用版本、實測結果、尚待驗證項目。
3. 產品影響或個人判斷：清楚區分外部事實、推論、建議與已核定的決策。
4. 未解問題與相關研究：待研究事項及其他筆記的相對連結。

新 Markdown 研究使用少量前置欄位，例如：

```yaml
---
title: AIDMS GPU 共享策略：MPS 與 MIG 比較
created: 2026-09-10
updated: 2026-09-10
verified: null
status: draft
tags: [gpu-sharing, mps, mig, multi-tenancy]
---
```

此日期只為格式示例，不應直接當成舊文件的研究日期。
`created` 為筆記建立日期，`updated` 為內容修改日期，`verified` 為最近核對來源的日期。
未知日期不可臆造；未查證填 `null`。匯入或轉檔不代表完成重新查證。
狀態應反映實際整理情況，不能以整理完成冒充事實已驗證。

## 分類與持續更新規則

- 同一研究只有一個主要位置，跨主題用連結引用，不複製多份正文。
- 通用知識放 `topics/`；針對 AIDMS 的具體應用、評估或決策放 `products/aidms/` 並連結通用研究。
- 最新資訊先放 `radar/`，記錄發布日期、來源、摘要與追蹤原因；值得深入的項目再形成主題研究。
- `radar/` 是有價值的時序紀錄；`inbox/` 是等待分類與整理的暫存區。
- 穩定主題使用英文小寫與連字號，例如 `gpu-sharing`；中文標題寫在正文及入口。
- 具時間意義的比較、實驗或快照才在名稱加 `YYYY-MM-DD`。
- 不使用 `final-v2-new` 之類命名。一般修訂交給 Git，值得保留的歷史快照另存獨立紀錄。
- 被取代的研究保留指向新研究的說明；必要時移入 `archive/`，並修復引用連結。
- 初期以導覽與文字搜尋為基礎；未來可擴充全文或語意搜尋，但不在此規劃階段預設建立搜尋服務。

## 寫作範本

`_templates/` 只放可複用的空白格式，不放完成的研究筆記：

- `research-note.md`：研究問題、摘要、證據、限制、来源、相關研究。
- `competitor-analysis.md`：競品定位、功能、規格、優缺點、查證日期。
- `comparison.md`：比較對象、評估條件、比較表、結論。
- `decision-record.md`：背景、候選方案、採用理由、影響與重新評估條件。

套用範本後的研究成果放回對應的 `products/` 或 `topics/`。

## Git 提交原則

研究知識庫的成果以 Git/GitHub 保存。以下內容預設應提交：

- `index.html`、其他研究 HTML 與網站導覽頁。
- `research.md`、其他 Markdown 研究筆記、README、範本與規劃文件。
- 研究需要的圖片、表格、PDF、簡報、文件與其他附件；若檔案過大，再個別評估儲存方式。
- `.gitkeep`，用來保留尚未有內容但已規劃的資料夾。

`.gitignore` 只排除本機作業系統檔案、編輯器設定、秘密設定、本機暫存、日誌、依賴套件與快取。
不要用忽略規則排除研究格式或某個主題目錄，避免重要研究內容未被 Git 發現。
若可重建的網站工具產生輸出，仍須依當次網站規劃決定是否提交；目前既有及規劃中的 HTML 閱讀頁面都應提交。

## 目前落實範圍

2026-09-10：使用者核定此規劃，要求記錄在工作區，並在此工作區執行任何動作前閱讀。
已建立規劃與 `AGENTS.md` 入口指示。

2026-09-10：依使用者要求，新增 `products/gpu/`，與 `products/aidms/` 並列，用於 GPU 產品研究、規格與比較。

2026-09-10：已建立上述規劃的分類資料夾與五個研究目錄，完成五份報告歸位。空分類目錄以 `.gitkeep` 保留，方便 Git/GitHub 追蹤；加入實際內容後可移除此佔位檔。

截至 2026-09-10，尚未建立 `index.html` 入口、Markdown 研究正文、寫作範本內容或月份筆記，也未設定網站生成、部署或執行 commit/push。目錄樹中的這些檔案仍為後續規劃，依使用者任務逐步落實。

2026-09-23：依使用者要求，建立 AIDMS 1.0／2.0 比較的 `research.md`、HTML 閱讀頁與該研究專用的生成腳本；全站共用生成流程仍未核定。使用者確認「閱讀 HTML、內容修正先改 Markdown 再重新產生 HTML；版面問題改版型；只有 HTML 的舊報告直接修改 HTML」，已記入上述修改流程。

2026-09-23：使用者進一步確認日後只會指出 HTML 的錯誤。代理須自行處理：內容錯誤先改 Markdown 並在同次任務同步 HTML；純 HTML 格式問題直接改 HTML。上述修改流程已依此更新。
